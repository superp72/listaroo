my_hash 

//set up
strings = [ /* ... */ ]
index = 0 
while index < stings.length
	# Do some stuff with stings[index]

	index += 1

	end

	strings = [ /* ... */ ]
index = 0 
while index < stings.length
	# Do some stuff with stings[index]

	index += 1

	end
	